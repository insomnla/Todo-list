CREATE TABLE IF NOT EXISTS `test`.`holydays` (
  `id_holidays` INT NOT NULL,
  `this_year_holydays` VARCHAR(100) NULL,
  PRIMARY KEY (`id_holidays`))
ENGINE = InnoDB


CREATE TABLE IF NOT EXISTS `test`.`vacation` (
  `id_vacation` INT NOT NULL AUTO_INCREMENT,
  `fk_id_worker` INT NULL,
  `start_date` VARCHAR(10) NULL,
  `end_date` VARCHAR(10) NULL,
  PRIMARY KEY (`id_vacation`),
  INDEX `fk_id_worker_idx` (`fk_id_worker` ASC) VISIBLE,
  CONSTRAINT `fk_id_worker`
    FOREIGN KEY (`fk_id_worker`)
    REFERENCES `test`.`worker` (`id_worker`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB

ЕСЛИ ВОРКБЕНЧ ЖАЛУЕТСЯ УДАЛИТЬ "CONSTRAINT 'fk_id_worker'" 

ДОБАВИТЬ В worker КОЛОНКУ vacation_left